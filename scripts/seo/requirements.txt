# Python deps for the seo-* skills (Phase 0 + 1 only — no paid APIs).
# Install with: pip install -r scripts/seo/requirements.txt
#
# Why pinned: claude-seo upstream pins to address known CVEs; we follow
# the same minimums. Loosen only if a transitive conflict appears.

beautifulsoup4>=4.12.0,<5.0.0
requests>=2.32.4,<3.0.0
lxml>=6.0.2,<7.0.0
urllib3>=2.6.3,<3.0.0
